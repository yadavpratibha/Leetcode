/**
 * Problem: 42. Trapping Rain Water
 * Source: https://leetcode.com/problems/trapping-rain-water/description/
 */

// Approach | Trapping Rain Water (LeetCode)

    //The amount of water trapped at any index depends on the minimum of the maximum heights on its left and right.
    //First, precompute a left max array, where each index stores the tallest bar seen from the left.
    //Then, precompute a right max array, where each index stores the tallest bar seen from the right.
    //For each index, calculate trapped water as: min(leftMax, rightMax) − height.
    //Sum this value for all indices to get the total trapped water.

class Solution {
    public int trap(int[] arr) {
		int n=arr.length;

        int[] left=new int[n];
        left[0]=arr[0];
        for(int i=1;i<n;i++){
            left[i]=Math.max(left[i-1],arr[i]);
        }

        int[] right=new int[n];
        right[n-1]=arr[n-1];
        for(int i=n-2;i>=0;i--){
            right[i]=Math.max(right[i+1],arr[i]);
        }

        int sum=0;
        for(int i=0;i<n;i++){
            sum=sum+Math.min(left[i],right[i])-arr[i];
        }
        return sum;
	}
}

//Time Complexity: O(n)
//Space Complexity: O(n)

